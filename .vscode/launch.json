{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
             "type": "node",
             "request": "launch",
             "cwd": "${workspaceRoot}",
             "args": [
                 "${workspaceRoot}/lib/app.js"
             ],
             "env": {
                 "ONE": "one"
             }
        }
    ]
}